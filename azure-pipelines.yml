trigger:
- '*'

variables:
  buildConfiguration: 'Release'
  releaseBranchName: 'release'
  webProjectDir: '$(Build.SourcesDirectory)/Tailspin.SpaceGame.Web'
  wwwrootDir: 'Tailspin.SpaceGame.Web/wwwroot'
  dotnetSdkVersion: '8.x'

stages:
- stage: 'Build'
  displayName: 'Build the web application'
  jobs:
  - job: 'Build'
    displayName: 'Build job'
    pool:
      name: 'SelfHosted-SecureAgents'
      demands:
      - npm

    steps:
    - task: NodeTool@0
      displayName: 'Use Node 18.x LTS'
      inputs:
        versionSpec: '18.x'

    - task: UseDotNet@2
      displayName: 'Use .NET SDK $(dotnetSdkVersion)'
      inputs:
        version: '$(dotnetSdkVersion)'

    # Install front-end deps
    - task: Npm@1
      displayName: 'npm install (web)'
      inputs:
        command: 'install'
        workingDir: '$(webProjectDir)'

    # Add sass (Dart Sass) to avoid node-gyp/native builds
    - task: Npm@1
      displayName: 'npm install sass (dev)'
      inputs:
        command: 'custom'
        workingDir: '$(webProjectDir)'
        customCommand: 'install --save-dev sass'

    # Compile Sass with dart-sass
    - task: CmdLine@2
      displayName: 'Compile Sass assets (dart-sass)'
      inputs:
        script: 'npx sass $(wwwrootDir):$(wwwrootDir) --no-source-map'
        workingDirectory: '$(webProjectDir)'

    - task: gulp@1
      displayName: 'Run gulp tasks'
      inputs:
        workingDirectory: '$(webProjectDir)'

    - task: CmdLine@2
      displayName: 'Write build info'
      inputs:
        script: 'echo $(Build.DefinitionName), $(Build.BuildId), $(Build.BuildNumber) > buildinfo.txt'
        workingDirectory: '$(webProjectDir)/wwwroot'

    - task: DotNetCoreCLI@2
      displayName: 'Restore project dependencies'
      inputs:
        command: 'restore'
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Build the project - $(buildConfiguration)'
      inputs:
        command: 'build'
        arguments: '--no-restore --configuration $(buildConfiguration)'
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Publish the project - $(buildConfiguration)'
      inputs:
        command: 'publish'
        projects: '**/*.csproj'
        publishWebProjects: false
        arguments: '--no-build --configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)/$(buildConfiguration)'
        zipAfterPublish: true

    - publish: '$(Build.ArtifactStagingDirectory)'
      artifact: drop
